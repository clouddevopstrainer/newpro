# --------------------- START MONITORING STACK ---------------------
- name: 🚀 Deploy Prometheus, Grafana, Node Exporter
  hosts: all
  become: yes
  tasks:
    - name: Ensure /opt/monitoring directory exists
      file:
        path: /opt/monitoring
        state: directory
        mode: '0755'

    - name: Clean old monitoring containers and free ports
      shell: |
        set -e
        echo "🧹 Cleaning old monitoring containers..."
        sudo docker ps -a --format '{{.Names}}' | grep -E 'monitoring-|prometheus|grafana|node_exporter' | xargs -r sudo docker rm -f || true

        echo "🧹 Freeing ports 3000, 9090, 9100..."
        for port in 3000 9090 9100; do
          pid=$(sudo lsof -t -i:$port || true)
          if [ -n "$pid" ]; then
            echo "⚠️ Killing process using port $port (PID: $pid)"
            sudo kill -9 $pid || true
          fi
        done

        echo "🧹 Cleaning Docker networks, volumes, and system..."
        sudo docker network prune -f || true
        sudo docker volume prune -f || true
        sudo docker system prune -f || true
      args:
        warn: false

    - name: Copy Prometheus configuration file
      copy:
        dest: /opt/monitoring/prometheus.yml
        content: |
          global:
            scrape_interval: 15s
          scrape_configs:
            - job_name: 'prometheus'
              static_configs:
                - targets: ['prometheus:9090']
            - job_name: 'node_exporter'
              static_configs:
                - targets: ['node_exporter:9100']

    - name: Copy docker-compose.yml for monitoring stack
      copy:
        dest: /opt/monitoring/docker-compose.yml
        content: |
          services:
            prometheus:
              image: prom/prometheus
              container_name: monitoring-prometheus
              ports:
                - "9090:9090"
              volumes:
                - ./prometheus.yml:/etc/prometheus/prometheus.yml
              restart: always

            grafana:
              image: grafana/grafana
              container_name: monitoring-grafana
              ports:
                - "3000:3000"
              environment:
                - GF_SECURITY_ADMIN_USER=admin
                - GF_SECURITY_ADMIN_PASSWORD=admin
              restart: always

            node-exporter:
              image: prom/node-exporter
              container_name: monitoring-node_exporter
              ports:
                - "9100:9100"
              volumes:
                - /proc:/host/proc:ro
                - /sys:/host/sys:ro
                - /:/rootfs:ro
              command:
                - '--path.procfs=/host/proc'
                - '--path.sysfs=/host/sys'
                - '--path.rootfs=/rootfs'
              restart: always

    - name: Start monitoring stack
      shell: |
        set -e
        cd /opt/monitoring
        echo "🚀 Deploying monitoring stack..."
        sudo docker compose up -d --remove-orphans
        echo "✅ Docker containers running:"
        sudo docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
      args:
        chdir: /opt/monitoring
